import VueRouter from "vue-router";
import Vue from "vue";

Vue.use(VueRouter);
import Home ()=

const routes=[

]

const router = new VueRouter({
  mode:'history',
  routes
})

export default router
